import { FormEvent, useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { ArrowLeft, ArrowUpRight } from 'lucide-react'; import { mockSignIn } from '../services/auth.service';
export function LoginPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const nav=useNavigate(); async function submit(e:FormEvent){e.preventDefault();setError('');try{await mockSignIn(email,password);nav('/chat')}catch(err){setError((err as Error).message)}} return <main className="grid min-h-[calc(100vh-80px)] place-items-center px-6 py-12"><div className="w-full max-w-md"><Link to="/" className="mb-12 inline-flex items-center gap-2 text-sm text-black/55"><ArrowLeft size={16}/>Back home</Link><p className="mono text-xs uppercase tracking-[.18em] text-[#04714a]">Welcome back</p><h1 className="mt-4 text-5xl font-bold tracking-[-.06em]">Sign in to Đông Đông.</h1><p className="mt-4 text-black/55">Use your university email to continue.</p><form onSubmit={submit} className="mt-10 flex flex-col gap-4"><input className="rounded-xl border border-black/15 bg-white px-4 py-3 outline-none focus:border-[#00a86b]" type="email" placeholder="you@pduni.edu.vn" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="rounded-xl border border-black/15 bg-white px-4 py-3 outline-none focus:border-[#00a86b]" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/>{error&&<p className="text-sm text-red-600">{error}</p>}<button className="rounded-xl bg-[#11130f] px-4 py-3 font-semibold text-white hover:bg-[#04714a]">Continue <ArrowUpRight className="ml-1 inline" size={16}/></button></form><p className="mt-6 text-sm text-black/55">New here? <Link className="font-semibold text-[#04714a]" to="/register">Create an account</Link></p></div></main>}
