import { FileText, UploadCloud } from 'lucide-react'; import { documents } from '../mocks/data'; export function DocumentsPage(){return <main><p className="mono text-xs uppercase tracking-[.18em] text-[#04714a]">Knowledge pipeline</p><h1 className="mt-3 text-5xl font-bold tracking-[-.07em]">Documents</h1><div className="mt-10 rounded-3xl border border-dashed border-black/20 bg-white p-10 text-center"><UploadCloud className="mx-auto text-[#00a86b]" size={30}/><h2 className="mt-4 font-semibold">Drop documents here</h2><p className="mt-2 text-sm text-black/50">PDF, DOCX, TXT, and Markdown · MarkItDown ready</p><button className="mt-6 rounded-xl bg-[#11130f] px-5 py-3 text-sm font-semibold text-white">Choose files</button></div><div className="mt-10 flex flex-col gap-2">{documents.map(d=><div key={d.id} className="flex items-center gap-4 rounded-2xl border border-black/10 bg-white p-4"><FileText className="text-black/35"/><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{d.name}</p><p className="mt-1 text-xs text-black/45">{d.type} · {d.size} · {d.updated}</p></div><span className={`text-xs ${d.status==='Ready'?'text-[#04714a]':'text-black/45'}`}>{d.status}{d.status==='Processing'&&` · ${d.progress}%`}</span></div>)}</div></main>}
