import { conversations } from '../mocks/data'; export function HistoryPage(){return <Page title="Chat history" eyebrow="Your conversations"><div className="flex flex-col gap-2">{conversations.map(c=><div className="flex items-center justify-between rounded-2xl border border-black/10 bg-white p-5" key={c.id}><span>{c.title}</span><span className="text-sm text-black/40">{c.date} →</span></div>)}</div></Page>}
function Page({title,eyebrow,children}:{title:string;eyebrow:string;children:React.ReactNode}){return <div><p className="mono text-xs uppercase tracking-[.18em] text-[#04714a]">{eyebrow}</p><h1 className="mt-3 mb-10 text-5xl font-bold tracking-[-.07em]">{title}</h1>{children}</div>}
